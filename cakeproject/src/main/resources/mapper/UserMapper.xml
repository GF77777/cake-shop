<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fr.mapper.UserMapper">

    <resultMap id="UserResult" type="com.fr.entity.User">
        <id column="id" property="id"/>
        <result column="userName" property="userName"/>
        <result column="passWord" property="passWord"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="address" property="address"/>
        <result column="isAdmin" property="isAdmin"/>
        <result column="isValidate" property="isValidate"/>
    </resultMap>

    <select id="getAll" resultMap="UserResult">
        SELECT id, userName, passWord, name, email, phone, address, isadmin as isAdmin, isvalidate as isValidate
        FROM user
        ORDER BY id DESC
    </select>

    <select id="login" resultMap="UserResult">
        SELECT id, userName, passWord, name, email, phone, address, isadmin as isAdmin, isvalidate as isValidate
        FROM user
        WHERE userName = #{userName} AND passWord = #{passWord}
    </select>

    <insert id="register">
        INSERT INTO user (userName, passWord, name, email, phone, address, isAdmin, isValidate)
        VALUES (#{userName}, #{passWord}, #{name}, #{email}, #{phone}, #{address}, #{isAdmin}, #{isValidate})
    </insert>

    <update id="update">
        UPDATE user SET
            userName = #{userName},
            name = #{name},
            email = #{email},
            phone = #{phone},
            address = #{address},
            isAdmin = #{isAdmin},
            isValidate = #{isValidate}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE user SET passWord = #{passWord} WHERE id = #{id}
    </update>

    <insert id="addUser">
        INSERT INTO user (userName, passWord, name, email, phone, address, isAdmin, isValidate)
        VALUES (#{userName}, #{passWord}, #{name}, #{email}, #{phone}, #{address}, #{isAdmin}, #{isValidate})
    </insert>

    <delete id="delete">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>